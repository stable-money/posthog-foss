"""No models of its own.

The five models under this app label live in `products/access_control/backend/models/`
and pin `app_label = "ee"` there. This module exists because Django only creates tables
for an app that has one.
"""
